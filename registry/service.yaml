apiVersion: v1
kind: Service
metadata:
  namespace: registry-ns
  name: registry-service
spec:
  type: ClusterIP
  selector:
    app: registry
  ports:
  - name: http
    protocol: TCP
    port: 5000
    targetPort: 5000
