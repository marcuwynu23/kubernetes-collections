apiVersion: v1
kind: Service
metadata:
  namespace: consul-ns
  name: consul-service
spec:
  type: ClusterIP
  selector:
    app: consul
  ports:
  - name: http
    protocol: TCP
    port: 8500
    targetPort: 8500
