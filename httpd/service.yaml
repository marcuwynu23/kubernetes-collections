apiVersion: v1
kind: Service
metadata:
  name: httpd-service
  namespace: k8s-collections
spec:
  selector:
    app: httpd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort