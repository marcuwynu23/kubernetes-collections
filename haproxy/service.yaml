apiVersion: v1
kind: Service
metadata:
  namespace: haproxy-ns
  name: haproxy-service
spec:
  selector:
    app: haproxy
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 80
  - name: stat
    protocol: TCP
    port: 8404
    targetPort: 8404
