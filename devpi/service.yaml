---
apiVersion: v1
kind: Service
metadata:
  name: devpi
  namespace: devpi

spec:
  type: ClusterIP

  selector:
    app: devpi

  ports:
  - name: http
    protocol: TCP
    port: 3141
    targetPort: 3141
