apiVersion: v1
kind: Service
metadata:
  namespace: code-server-ns
  name: code-server-service
spec:
  selector:
    app: code-server-deployment
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8449
