---
# ============================================================
# pgAdmin Service
# ============================================================

apiVersion: v1
kind: Service
metadata:
  name: supabase-ui
  namespace: supabase
spec:
  type: NodePort

  selector:
    app: supabase-ui

  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30050
