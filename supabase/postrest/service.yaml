---
# ============================================================
# PostgREST Service
# ============================================================

apiVersion: v1
kind: Service
metadata:
  name: supabase-rest
  namespace: supabase
spec:
  type: ClusterIP

  selector:
    app: supabase-rest

  ports:
  - name: http
    port: 3000
    targetPort: 3000
